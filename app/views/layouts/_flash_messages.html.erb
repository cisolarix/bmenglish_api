<% flash.each do |type, message| %>
  <%= content_tag :div, class: "alert #{type} alert-dismissable" do %>
    <%= button_tag 'x', class: 'close', data: { "dismiss" => "alert" } %>
    <%= message %>
  <% end %>
<% end %>
